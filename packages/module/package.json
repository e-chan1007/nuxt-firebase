{
  "name": "@e-chan1007/nuxt-firebase-module",
  "scripts": {
    "prepack": "rm -rf ../../dist && nuxt-module-build && cp -LR ./dist ../../dist",
    "dev:prepare": "rm -rf ../../dist && nuxt-module-build --stub && cp -LR ./dist ../../dist"
  }
}
