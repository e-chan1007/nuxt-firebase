{
  "name": "@e-chan1007/nuxt-firebase-module",
  "scripts": {
    "prepack": "nuxt-module-build && cp -LRT ./dist ../../dist",
    "dev:prepare": "nuxt-module-build --stub && cp -LRT ./dist ../../dist"
  }
}
