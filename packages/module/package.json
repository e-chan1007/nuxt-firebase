{
  "name": "@e-chan1007/nuxt-firebase-module",
  "scripts": {
    "prepack": "rm -rf ../../dist && nuxt-module-build && cp -LRT ./dist ../../dist",
    "dev:prepare": "rm -rf ../../dist && nuxt-module-build --stub && cp -LRT ./dist ../../dist"
  }
}
